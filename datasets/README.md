# Generated datasets

Dataset configuration and manifests are versioned here. Recorded videos, runtime state,
logs, reports, and converted LeRobot shards are generated locally and ignored by Git.

The default collection root is `datasets/urban-vla-expert-v1`. Set
`VLA_DATASET_ROOT` or pass `--root` to place large data on another disk.
